name: field_surveillance_robot
description: A field surveillance robot control interface
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  geolocator: ^10.1.0
  intl: ^0.19.0
  camera: ^0.10.5+9
  sensors_plus: ^4.0.1
  flutter_joystick: ^0.2.2
  firebase_core: ^2.24.2
  firebase_database: ^10.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
